apiVersion: v1
kind: PersistentVolume
metadata:
  name: misago-redis-pv
  labels:
    app: misago
spec:
  storageClassName: ""
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: db.pi.cluster
    path: "/data/share/misago/redis-pv"
  mountOptions:
    - nfsvers=4.2
