apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: misago
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 1Gi
  volumeName: misago-media-pv
